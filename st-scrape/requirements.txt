# Pinned exactly: both Fargate images (Dockerfile, Dockerfile.curate) install this
# file, so a floating floor rebuilds the image asset and replaces the ECS task
# definitions on the next unrelated CDK deploy. Bump deliberately.
requests==2.34.2
beautifulsoup4==4.14.3
boto3==1.43.20
tzdata==2026.2
pyarrow==24.0.0
duckdb==1.5.4
